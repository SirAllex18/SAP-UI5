<mvc:View
    controllerName="sap.ui.demo/walkthrough.controller.App"
    xmlns="sap.m"
    xmlns:mvc="sap.ui.core.mvc"
    displayBlock= "true"> 


  <Shell>
    <App class="myAppDemoWT" id="app"/>
  </Shell>
</mvc:View>

    <!--Moved this to OverView.view for routing example
      <pages>
        <Page title="{i18n>homePageTitle}">
        <headerContent>
            <Button
              icon="sap-icon://hello-world"
              press=".onOpenDialog"/>
        </headerContent>
          <content>
            <mvc:XMLView
              viewName="sap.ui.demo.walkthrough.view.HelloPanel"/>
            
            <mvc:XMLView
              viewName="sap.ui.demo.walkthrough.view.InvoiceList"/>
    -->          
 <!-- Nested views - > moved the panel to it`s own view + own controller 
                       For better code structure
            
        <Panel headerText="{i18n>helloPanelTitle}"
                class="sapUiResponsiveMargins"
                width="auto">
          <content>
               <Button text="{i18n>showHelloButtonText}"
                       press=".onShowHello"
                       class="myCustomButton"/>

               <Input value="{/recipient/name}"
                      description="Good bye {/recipient/name}"
                      valueLiveUpdate="true"
                      width="60%"/>
               <FormattedText
                  htmlText="Hello {/recipient/name}"
                  class="sapUiSmallMargin sapThemeHighlight-asColor myCustomText"/>
          </content>

        
          </content>

        </Page>
      </pages>
  -->


  <!-- the controls defined, where it is placed is decided in JS file-->